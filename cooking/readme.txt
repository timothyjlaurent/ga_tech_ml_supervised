to run the software pull from the repo:

git pull https://github.com/timothyjlaurent/ga_tech_ml_supervised

ensure that you have a scientific python installed like anaconda.

start a virtual environment

virtualenv ve && source ve/bin/activate

install dependencies

pip install -r requirements.txt

then start the noteboo:

ipython notebook

Then files in the folders, cooking and titantic conain the freshest code
